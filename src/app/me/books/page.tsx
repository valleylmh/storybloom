import DeviceCloudStoryLibrary from "@/components/account/DeviceCloudStoryLibrary";

export const metadata = {
  title: "我的绘本 | StoryBloom",
};

export default function MyBooksPage() {
  return <DeviceCloudStoryLibrary />;
}
